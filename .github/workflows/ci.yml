# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# permissions:
#   contents: read

# jobs:
#   lint:
#     name: Lint & Type Check
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Install dependencies
#         run: |
#           pip install --upgrade pip
#           pip install -e ".[dev]"

#       - name: Ruff lint
#         run: ruff check src/ tests/ mcp_server.py

#       - name: Ruff format check
#         run: ruff format --check src/ tests/ mcp_server.py

#       - name: Mypy type check
#         run: mypy src/ --ignore-missing-imports

#   test:
#     name: Test (Python ${{ matrix.python-version }})
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: ["3.10", "3.11", "3.12"]
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}

#       - name: Install dependencies
#         run: |
#           pip install --upgrade pip
#           pip install -e ".[dev]"

#       - name: Run tests
#         run: pytest -m "not integration" --tb=short -q

#       - name: Run tests with coverage
#         if: matrix.python-version == '3.12'
#         run: pytest -m "not integration" --cov=src --cov-report=term-missing --cov-fail-under=60

#   docker:
#     name: Docker Build
#     runs-on: ubuntu-latest
#     needs: [lint, test]
#     steps:
#       - uses: actions/checkout@v4

#       - name: Build Docker image
#         run: docker build -t aws-tagging-utils:ci .

#       - name: Smoke test
#         run: |
#           docker run --rm aws-tagging-utils:ci python -c "from src.config import DEFAULT_REGION; print(f'Config OK: {DEFAULT_REGION}')"
